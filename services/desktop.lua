local dialogWindows = require("dialogWindows")
local system = require("system")
local gui = system.gui

dialogWindows.message(scene, "liteOS info", "a new os!\nit is still in early access", gui:getColor("lightGray"))