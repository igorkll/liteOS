return {create = function(gpuaddress)
    local invoke = component.invoke

    local foregrounds = {}
    local backgrounds = {}
    local chars = {}

    -----------------------------------------

    local function clear()
        
    end

    local function update()
        
    end

    -----------------------------------------

    return {
        clear = clear,
        update = update,
    }
end}